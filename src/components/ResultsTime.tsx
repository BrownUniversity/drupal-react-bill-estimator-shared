import styled from "styled-components";
import { colors, typography } from "brown-university-styles";

const ResultsTime = styled.p`
  font-family: ${typography.sans};
  font-size: 14px;
  text-align: center;
  color: ${colors.mediumGray};
  margin-bottom: 60px;
`;

export default ResultsTime;
